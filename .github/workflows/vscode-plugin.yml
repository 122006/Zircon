name: VS Code plugin

on:
  push:
    paths:
      - "base/**"
      - "javac/**"
      - "vscode_plugin/**"
      - "vscode_plugin_agent/**"
      - "build.gradle"
      - "settings.gradle"
      - "gradle/**"
      - "gradlew*"
      - ".github/workflows/vscode-plugin.yml"
  pull_request:
    paths:
      - "base/**"
      - "javac/**"
      - "vscode_plugin/**"
      - "vscode_plugin_agent/**"
      - "build.gradle"
      - "settings.gradle"
      - "gradle/**"
      - "gradlew*"
      - ".github/workflows/vscode-plugin.yml"

jobs:
  validate:
    runs-on: windows-latest
    steps:
      - uses: actions/checkout@v4

      - name: Set up Java 8 toolchain
        uses: actions/setup-java@v4
        with:
          distribution: temurin
          java-version: "8"

      - name: Set up Java 17
        uses: actions/setup-java@v4
        with:
          distribution: temurin
          java-version: "17"

      - uses: actions/setup-node@v4
        with:
          node-version: "22"
          cache: npm
          cache-dependency-path: vscode_plugin/package-lock.json

      - uses: gradle/actions/setup-gradle@v4

      - name: Validate extension
        working-directory: vscode_plugin
        run: |
          npm ci
          npm test

      - name: Validate JDT agent
        run: .\gradlew.bat :vscode_plugin_agent:test :vscode_plugin_agent:shadowJar :vscode_plugin_agent:bootstrapHelperJar

      - name: Assemble extension artifacts
        run: .\gradlew.bat :vscode_plugin_agent:buildZirconVsCodePlugin

      - name: Set up Java 22 for native JDT probes
        uses: actions/setup-java@v4
        with:
          distribution: temurin
          java-version: "22"

      - name: Install Red Hat Java language server
        run: code --install-extension redhat.java --force

      - name: Validate native JDT integration
        working-directory: vscode_plugin
        run: |
          $env:JDK22_HOME = $env:JAVA_HOME
          npm run probe:indexed-completion
          npm run probe:native-search
          npm run probe:jdt-diagnostics
          npm run probe:template

      - name: Validate VSIX packaging
        working-directory: vscode_plugin
        run: npx vsce package --allow-missing-repository
