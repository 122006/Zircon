plugins {
    id 'java'
}

// The original Java files contain Zircon syntax. The compiler fixture runner
// owns their compilation, so negative fixtures cannot break compileTestJava.
sourceSets.test.java.setSrcDirs([])

tasks.named('test') {
    dependsOn ':javac:compilerRegression'
}
